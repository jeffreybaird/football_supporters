<span class="badge<%= " badge--sm" if defined?(sm) && sm %>">
  <% if team.crest %>
    <img src="<%= crest_url(team.crest) %>" alt="<%= team.name %> crest">
  <% else %>
    <span class="badge__initials"><%= team_initials(team.name) %></span>
  <% end %>
</span>
